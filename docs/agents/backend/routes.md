# Routes

Per-endpoint reference for backend routes, one file per domain, complementing the
entity/event-focused `docs/agents/modules/` pages.

- [Auth](routes/auth.md)

Future backend modules add their own entry here as they land.
