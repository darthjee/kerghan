# Update architecture docs
Document the new `utils/validation/` folder wherever `utils/` subfolders are listed.

## Files to Change
- `docs/agents/architecture/frontend.md` — add `validation/  # fieldValidators, formValidators (shared client-side form rules)` under `utils/` in the folder tree (and any prose listing the controllers under `accounts/pages/`, if it mentions `ResetPasswordController`)
- `docs/agents/folder-structure.md` — add `utils/validation/` (shared form validators) to the `assets/js/` row alongside `utils/routing/` and `utils/polling/`
